<h2>プロフィール</h2>

<% if @user.image? %>
  <%= image_tag @user.image.url, class: "profile-image", style: "border-radius: 30%; width: 150px; height: 150px; object-fit: cover;" %>
<% else %>
  <%= image_tag 'icon.png', alt: 'Default Icon', width: '100', height: '100' %>
<% end %>

<p><strong>名前:</strong> <%= @user.name %></p>
<p><strong>自己紹介:</strong> <%= @user.profile %></p>
