<h1>検索結果</h1>
<p><%= @rooms.count %>件の施設が見つかりました</p>
<table>
  <thead>
    <tr>
      <th>施設画像</th>
      <th>施設名</th>
      <th>住所</th>
      <th>紹介文</th>
      <th>宿泊料金 (/日)</th>
    </tr>
  </thead>
  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td>
          <% if room.room_image.attached? %>
            <%= link_to room_path(room) do %>
              <%= image_tag(room.room_image, size: '100x100') %>
            <% end %>
          <% else %>
            <%= link_to room_path(room) do %>
              <%= image_tag('image.png', alt: 'デフォルト画像', size: '100x100') %>
            <% end %>
          <% end %>
        </td>
        <td><%= link_to room.room_name, room_path(room) %></td>
        <td><%= room.address %></td>
        <td><%= room.introduction %></td>
        <td><%= room.price %>円</td>
      </tr>
    <% end %>
  </tbody>
</table>
